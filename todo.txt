- be able to configure terminal colors
- find a solution to the per-user rc file problem
- add SBIN to the path if the user is root
- add 'uninstall' target
- improve the build system to treat each script individually
- name USER=idank install
